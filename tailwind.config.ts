import type { Config } from "tailwindcss";

const config: Config = {
  darkMode: "class",
  content: [
    /* your globs */
  ],
  theme: {
    extend: {
      // Optional: custom tokens if needed
    },
  },
  plugins: [],
};

export default config;
